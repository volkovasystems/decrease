"use strict";

const assert = require( "assert" );
const decrease = require( "./decrease.js" );

let array = [ 1, 2, 3 ];
let result = decrease( array );
assert.ok( result );
assert.ok( decrease( ) );

console.log( "ok" );
